<h1>Спсисок працівників департменту</h1>

<table class="table">
  <tr>
    <th>ПІБ</th>
    <th>Статус</th>
    <th>Департамент</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employees.each do |employee| %>
  <tr>
    <td><%= employee.name %></td>
    <td><%= employee.active %></td>
    <td><%= employee.department %></td>
    <td><%= link_to 'Show', [employee.department, employee] %></td>
    <td><%= link_to 'Edit', edit_department_employee_path(employee.department, employee) %></td>
    <td><%= link_to 'Destroy', [employee.department, employee], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Додати працівника', new_department_employee_path(params[:department_id]) %>
