<%= form_for([@employee.department, @employee]) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Прізвище Імя по Батькові" %><br />
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :active, "Активний статус" %><br />
    <%= f.check_box :active %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
